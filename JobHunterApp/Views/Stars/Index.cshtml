@model IEnumerable<JobHunterApp.Models.Star>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.StarAmount)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.StarAmount)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.StarsID }) |
            @Html.ActionLink("Details", "Details", new { id=item.StarsID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.StarsID })
        </td>
    </tr>
}

</table>
